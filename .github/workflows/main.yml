name: GitHub Actions - install check
on:
  pull_request:

jobs:
  install-check:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: 
          - ubuntu-latest
          - macos-latest
    steps:
    - name: clone
      uses: actions/checkout@v2
    - uses: ./
    - run: infer --help
